{
  "name": "monorepo-this-name-is-not-used",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "yarn workspaces run clean",
    "lint": "yarn workspaces run lint",
    "package": "yarn workspaces run package",
    "test": "yarn workspaces run test"
  }
}
